extends ./../layout/index
block content 
  div(class="py-10")
    h1(class="text-4xl my-10 font-extrabold text-center") Bienes
      span(class="font-normal") Raices
    h2(class="text-center text-2xl")= page

    if errors
      div(class="max-w-md mx-auto my-10")
        each error in errors
          p(class="bg-red-600 text-white uppercase text-xs text-center p-2 mb-1 font-bold")= error.msg

    div(class="mt-8 mx-auto max-w-md")
      div(class="bg-white py-8 px-4 shadow rounded-md")
        form(class="space-y-5" method="POST" action="/auth/signup" noValidate)
          input(type="hidden" name="_csrf" value= csrfToken)
          div
            label(class="block text-sm uppercase text-gray-500 mb-3 font-bold" for="name") Nombre
            input(id="name" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Luis" type="text" name="name" value= user ? user.name : "")
          div
            label(class="block text-sm uppercase text-gray-500 mb-3 font-bold" for="email") Correo
            input(id="email" class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="luis@gmail.com" type="email" name="email" value= user ? user.email : "")
          div
            label(class="block text-sm uppercase text-gray-500 mb-3 font-bold" for="password") Contraseña
            input#password(class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Contraseña" type="password" name="password")
          div
            label(class="block text-sm uppercase text-gray-500 mb-3 font-bold" for="repeat_password") Repetir contraseña
            input#repeat_password(class="w-full px-3 py-2 border border-gray-300 rounded-md placeholder-gray-400" placeholder="Repetir contraseña" type="password" name="repeat_password")
          div(class="flex items-center justify-between")
            a(class="text-gray-500 text-sm" href="/auth/login") ¿Ya tienes cuenta? Iniciar sesión 
            a(class="text-gray-500 text-sm" href="/auth/recovery") Olvidé mi contraseña 
          input(class="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 cursor-pointer rounded-md" type="submit" value="Crear cuenta")